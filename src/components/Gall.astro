---
import { type CollectionEntry } from "astro:content";
import { Image } from "astro:assets";

type Props = {
    gall: CollectionEntry<'galleryStock'>;
};

const { gall } = Astro.props;
---
<!-- <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
    <div class="grid gap-4"> -->

<div>
    <a href=`/gall/${gall.slug}`>
        <img class="h-auto max-w-full rounded-lg" src={gall.data.image} alt="">
    </a>
</div>